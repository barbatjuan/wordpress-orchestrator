# House-rules checklist (the gate for the orchestrator's defaults)

The orchestrator's "House rules" (`agents/wordpress-orchestrator.md`) had no verification
gate. This is it. Run every row. Report PASS / FAIL / UNVERIFIED per row — never blank.

How verification works here: the sandbox domain is usually browser-blocked, so checks fetch
the compiled CSS / front HTML server-side and count selectors and strings, or read WordPress
options directly. Rows marked **eyes** cannot be proven that way — ask the user and say so.
Never claim a visual result you did not see.

Run every per-page row on EVERY page in scope, not just the home.

| # | Rule | What to check | Server-side method | Verdict source |
|---|------|---------------|--------------------|----------------|
| 1 | Currency = euros | Store currency is `EUR` and the symbol rendered is `€` | Read the live option `woocommerce_currency` (and the active currency symbol). Then fetch a page with prices and count `€` vs `$` / `USD` in the price markup | **auto** |
| 2 | Cart is an icon + count badge | The header cart renders a glyph and a quantity bubble, and NO text label | Isolate the cart fragment in the header HTML (`elementor-menu-cart`). PASS needs: an icon node inside the toggle (`eicon-…` class or an inline `<svg>`) AND the quantity-indicator node. FAIL on any hit for `Carrito`, `Cesta`, `Bolsa`, `Bag`, `Cart` as visible text inside that fragment. Cross-check the widget config: `items_indicator:'bubble'`, `toggle_button_border_width:0` | **auto** |
| 3 | Theme | Elementor build on Hello Elementor; existing Astra / GeneratePress kept, not swapped; Divi build on the Divi theme | Read the active theme (`stylesheet` / `template` options). Compare against what `project-context` reported BEFORE the build — a changed value on an existing site is a FAIL | **auto** |
| 4 | Logo → home | The header logo is wrapped in an anchor whose href is the site home URL, on every page | Isolate the header fragment, take the anchor around the logo widget, compare its href to the live home URL. FAIL on `#`, empty, `javascript:`, or any non-home target. Repeat per page | **auto** |
| 5 | Exactly ONE menu | One nav-menu widget instance per page — no second nav, no duplicated item | Count nav-menu WIDGET instances in the header, not `<nav>` elements. Trap: one Elementor nav-menu widget renders TWO lists (`elementor-nav-menu--main` + `elementor-nav-menu--dropdown`); counting those gives a false FAIL. Confirm the exact widget attribute by introspecting the rendered header once, then record it in `elementor-core/references/knowledge.md`. Also dedupe the item labels/hrefs — a repeated href is a duplicated item | **auto** |
| 6 | No dead links | Every menu item resolves | Extract the hrefs from the menu markup, request each server-side (HEAD, follow redirects), flag anything not 2xx/3xx. Also flag `#` and empty hrefs | **auto** |
| 7 | Header present on every page | No page loses its header | Fetch every page in scope; the header fragment must be present in all of them. A missing header usually means a Theme Builder condition gap or a leftover template hijack — check conditions before reporting | **auto** |
| 8 | Header sticky | The header stays visible on scroll | Config only: confirm the sticky setting is stored on the header container (`sticky` = top, enabled for desktop/tablet/mobile). The sticky class is applied by JS at runtime, so the stored setting is the ONLY server-side proof — the behaviour itself is **eyes** | **auto (config) + eyes (behaviour)** |
| 9 | Header/footer reused verbatim | One global header and one global footer, byte-identical across pages | Extract the header fragment and the footer fragment from each page, normalise whitespace, hash them. All header hashes must match; all footer hashes must match. A mismatch = a per-page copy crept in. Also confirm there is ONE header template and ONE footer template with a single site-wide condition | **auto** |
| 10 | Mobile 3-zone header (burger · logo · cart) | Burger left, logo centred, cart right, all vertically centred, below 768px | Two layers. (a) Server-side: the mobile-only rules from `elementor-core/references/gotchas.md` ("Mobile 3-zone header") exist in the compiled `post-<id>.css` — count the positioning-context, absolute-centred logo, `space-between` cluster and pinned-actions rules. (b) Geometry: measure with the in-app browser under mobile emulation reading each zone's bounding rect (bust the CSS cache with a `?v=` query). Rules present but never measured = UNVERIFIED, not PASS | **auto (rules) + measured or eyes (layout)** |

| 11 | Fewest containers that do the job | No container nested "just because": none empty, none wrapping a single widget without carrying its own background/border/shadow or boxing that widget where no ancestor already does, no section whose only child is a flex row (the section IS the row), no photo living as a container `background_image`, and nothing past depth 3 | `require_once` the sandbox's `es-builder.php` and **call `es_container_audit()`** on the `json_decode`d `_elementor_data` of each page AND each Theme Builder template in scope. Do NOT re-implement the walk here in prose or in a throwaway script: two implementations of one rule drift, and the one you hand-roll is the one that will be wrong. This runs against what actually LANDED, not what was sent — that is the whole reason it repeats a check the build already did. Report `containers / widgets / max_depth` per page and list every offender by path. PASS needs zero offenders **and** an empty `unaudited` map: a non-empty one means part of that page is built from elTypes this walk has no opinion about (pre-3.6 `section`/`column`, a kit import), so report it as UNVERIFIED for that page — zero offenders over a tree nobody judged is not a pass. `optimizable` entries (a container whose only child is a grid) are reported as a **note**, never a FAIL — that call needs a human. W1 or W3 only. This reads STORED data, and no HTTP request can see stored data: on a production with no ephemeral-PHP arm there is no way to run this row there at all. Prove it locally before the transfer and report production UNVERIFIED. Nothing replaces it: the local pass is the evidence, and the migration plugin copying the site faithfully is the assumption that carries it across | **auto** |
| 12 | Exactly one H1 per page | Every page in scope has exactly one `<h1>`, and it describes that page | `wordpress-seo` states this as a Hard Rule but writes no checker, so it was unenforced. Fetch each page's front HTML and count `<h1` occurrences. FAIL on 0 (nothing tells a crawler what the page is) and on 2+. Also flag an H1 whose text is the site name on every page instead of the page's own subject — technically one H1, functionally none | **auto** |

| 13 | Accessibility is measured, not eyeballed | Lighthouse accessibility ≥ 90 on every page in scope, mobile | `node ../assets/lighthouse-audit.mjs <url…>` (from `qa-review/assets/`). It names the failing audits, not just the score: contrast, `image-alt`, `link-name`, `button-name`, `heading-order`, `html-has-lang`, `target-size`. Under 50 blocks. This REPLACES eyeballing step 4 — a11y was five rules with no method until this existed. Lighthouse cannot judge whether alt text is *meaningful*, only that it is present: that part stays **eyes** | **auto (+ eyes for alt quality)** |
| 14 | Best practices + SEO | Lighthouse best-practices ≥ 90 and SEO ≥ 90 | Same run. Catches console errors, mixed content, wrong image aspect ratios, a missing `<title>` or meta description, uncrawlable anchors. Under 50 blocks. Row 12 (one H1) still runs separately — Lighthouse does not check H1 count | **auto** |
| 15 | Performance is reported, never the sole blocker | Lighthouse performance score + LCP / CLS / TBT recorded for every page in scope | Same run. **Deliberately non-blocking**: mobile Elementor rarely reaches 90, and a gate that always fails is a gate everyone learns to skip. What matters is the before/after delta `wordpress-performance` owns — so record the number here and hand it over. A score from a sandbox host is not a score from the client's production host; say which one you measured | **auto (number) + judgement (verdict)** |
| 16 | The site's front page is the page that was built | `/` serves the home page this build made — not the blog archive, not somebody else's page | Two layers, because an option is intent and a response is proof. (a) Read `show_on_front` and `page_on_front` live, or call `es_front_page()` from the sandbox's `es-builder.php`, which is the one resolver. FAIL when `show_on_front` is `posts` — the site serves the blog at `/` regardless of what was built — when `page_on_front` is `0`, or when it resolves to a missing/trashed post. Half the setting is the same as none of it: `page_on_front` alone does nothing. Then compare that id against the home page the build reported; a different id means the built home is published but is not the front page. (b) Fetch `/` and confirm the HTML is that page's — match the `elementor-<id>`/`page-id-<id>` body class and the `<h1>` against the page — because a plugin, an options cache or a Theme Builder condition can serve something else over a correctly written option. Options read but `/` never fetched = UNVERIFIED, not PASS. Worlds: (a) is W1 or W3, (b) is W2, and the COMPOSITION is the row. This is the pattern the rest of this file follows — one layer proven where the options live, the other where the visitor is, and neither alone | **auto** |
| 17 | No live orphans, and old URLs go somewhere | A slug that moved leaves no second page competing for it, and its old URL redirects instead of 404ing | Read the `es_slug_redirects` option — `{old_slug: new_slug}`, written by `es_migrate_slug()`. For each pair: (a) confirm exactly ONE page answers the new slug, and that NO published page still sits at the old one; a live page at both is the orphan this row exists for, and it is usually the one search results still point at. (b) Request `/<old>/` server-side WITHOUT following redirects and read the status: `301` is a PASS, `404` and `200` are both FAILs — a `200` means the orphan is still being served. **Expect the FAIL until a human closes it**: nothing in this framework serves that map, and nothing in it can — writing `.php` outside the sandbox is forbidden, and the sandbox is emptied at hand-off, so anything it installed would be deleted by its own delivery phase. The two ways out are a redirect plugin or the mu-plugin in `elementor-core/references/knowledge.md` ("Servir es_slug_redirects"); `es_migrate_slug()` names that file on every successful move, and this row is what confirms somebody acted on it. An empty/missing option means no slug moved in this build — report N/A, never PASS. Also cross the page inventory against the slugs the build wrote: a published page at a slug the build did not intend is an orphan whether or not it appears in the map. Worlds: the rename itself is W1 or W3, since `es_migrate_slug()` is a library call; the redirect it leaves behind is proven in W2 by requesting the old path and requiring a 301. After a transfer the serving mu-plugin travels in wp-content/mu-plugins and lands as an ordinary file of the client's, which is the first arrangement in which this row can pass at all | **auto** |
| 18 | A form that does not deliver is not a form | One real submission ARRIVES, and the recipient is an address the client can read | Two things, and the second one is the one that is usually wrong. (a) **Recipient**: read it back from the plugin's stored config — Elementor Pro `form_fields`/`email_to`, WPForms `settings.notifications`, CF7's mail template, whichever `project-context` found. FAIL when it is empty or equals `admin_email`: most plugins fall back to it, and on a fresh install that is the developer's or the host's address, so every lead lands somewhere the client never looks. (b) **Delivery**: submit once with a unique marker string, then prove ARRIVAL — in the inbox the client provided, or in the plugin's stored entries. Say WHICH of the two you proved and never claim both. A rendered form that validates and shows its success message while the mail silently fails is indistinguishable from a working one from the outside, so a submission that was sent but never found is UNVERIFIED, not PASS. No form in scope = N/A. (b) is production-only, and a local PASS on it is a LIE. Local mail is trapped in a catcher or dead, so "the message arrived" cannot be proven before the transfer no matter how green the local run looks. Report (b) UNVERIFIED until it is proven on production, where it needs an email and a human reading an inbox — which is not a bridge | **auto (recipient) + auto or eyes (arrival)** |
| 19 | Consent is required, and its link is alive | The consent checkbox exists, is required, starts unticked, and its privacy link resolves | Isolate the form markup: the consent field must be present, carry the plugin's required flag, and NOT be pre-checked — a pre-ticked box is not consent in any reading of the GDPR. Then take the href out of its label and request it server-side following redirects; anything not 2xx is a FAIL. A consent box pointing at a 404 is worse than no box: it looks compliant and collects the data anyway. Check the privacy page has real content, not a placeholder — `wordpress-legal` owns writing it, this row owns proving the link goes somewhere real. No form in scope = N/A | **auto** |
| 20 | Legal pages exist, are reachable, and describe THIS site | Legal notice, privacy and cookies exist, are linked from every footer, and the cookie policy names the services the site actually loads | Three parts. (a) Request each legal page: 2xx and non-empty. (b) Extract the footer fragment of every page in scope and require a link to each legal page in ALL of them, then request each link — a legal page nobody can reach is a file, not compliance. (c) Collect the third-party HOSTS the built pages actually request (analytics, fonts, maps, chat, pixels) and require each one to appear in the cookie policy text. A policy naming services the site does not use is as false as one omitting the ones it does; report both directions, and treat an omission as the FAIL. Placeholder identity data — `[NOMBRE]`, `XXX`, `lorem`, an unfilled tax id — is a FAIL, not a nit: a legal page naming the wrong entity looks done, which is why nobody re-reads it | **auto** |
| 21 | The consent banner blocks before it asks | No non-essential third-party request fires before consent is given | Load one page in a FRESH context with no cookies and no local storage, and list every outbound request before touching the banner. Anything to a known analytics/ads/social host is a FAIL: styling a notice while firing the tracker underneath it is the failure this row exists for, and it is the normal state of most installs. Then accept and confirm the same requests DO fire — a banner that blocks permanently is a broken integration, not a compliant one. Also check reject is as reachable as accept (present, one click, not hidden behind a settings pane). No consent plugin active = FAIL when third-party trackers load at all, N/A when the site loads none | **auto (requests) + eyes (reject prominence)** |
| 22 | The sandbox is empty at hand-off | `wp-content/novamira-sandbox/` holds nothing after delivery | Call `es_sandbox_report()` and require an EMPTY list. Every `.php` in that directory executes on upload — that is how this framework runs — and anything left there stays executable and reachable by URL on the client's site indefinitely, including whatever was pasted in to debug something once. `es_sandbox_purge()` deletes and then RE-READS, returning what survived, so the proof is the re-read and never the delete: a purge blocked by permissions and one that worked are indistinguishable from `unlink()`'s return alone. Files it deliberately will not touch — subdirectories, extensions this framework never uploads, and any file registering a WordPress hook — are still FAILs here; they just need a human. The hook case was found cleaning a real client's sandbox: a file hooking `template_redirect` wrapped every page in the `<main>` landmark the theme does not print, so it was the site's accessibility and not build scaffolding, and the purge would have deleted it on hand-off day in silence with every check green. The warning names the hooks it found. The remedy is to MOVE such a file into the child theme and delete it here afterwards, never before — this framework may not write PHP outside the sandbox, so a human does that part. Non-empty = FAIL, and it blocks delivery. Bridge-free arm, for a production that runs none of this. The real proof moves EARLIER and becomes row 33: assert against the archive's own file listing that nothing under `novamira-sandbox/` was ever shipped. Then corroborate over HTTP, keying on the STATUS CODE ONLY. Request `/wp-content/novamira-sandbox/es-builder.php`: **404 is the only PASS**; **403 is not a pass**, because it proves the server declines to serve and not that the file is absent, and a host config change turns it into a 200 tomorrow; **200 is a FAIL, empty body included** — that is the trap, since `es-builder.php` exits without `ABSPATH` and a naive probe reads its empty 200 as "nothing there". Calibrate first with a random 32-hex filename: if THAT returns 200 the host soft-404s, the probe is uncalibrated, and this row is UNVERIFIED. Worlds: the library call is W1 or W3; the HTTP probe above is W2; the archive listing (row 33) is W1 and offline | **auto** |
| 23 | The backup keys are handed over, and indexing is declared | The client receives the restore keys, and whether the site is indexable is stated as a fact | Two facts nobody ever wrote down. (a) `es_backup_keys($ids)` over every page in scope returns `{post_id: [key, …]}`, newest last; put that list in the hand-off, **with the restore call beside it**: `es_restore_page_state($id)` for the newest, or `es_restore_page_state($id, '<key>')` for a specific one. "There is a backup" is not a deliverable, and neither is a key with no way to use it — until that function existed the hand-off was a list of meta keys and a sentence telling a human to restore them one by one, remembering that `_elementor_data` needs `wp_slash()` and the CSS needs regenerating, at the exact moment something had already gone wrong. Mention `es_prune_backups($id, $keep)` too when a page has been rebuilt many times: the keys are never pruned by default and each holds the whole displaced state. A page with no keys means it was never overwritten, which is worth saying rather than omitting. (b) `es_indexing_state()` reads `blog_public`: `0` is WordPress's "discourage search engines", which every staging site is built with and nobody remembers to switch off, so the site is delivered looking perfect and stays invisible for weeks. State the value and the verdict explicitly, and FAIL a delivery claiming SEO work on a site with `blog_public` = 0. That function deliberately does NOT parse robots.txt — fetch `/robots.txt` over HTTP here and read it, because a virtual one is invisible from disk. Bridge-free arm. Set `blog_public` LOCALLY, before the export, and let it travel in the dump; production's `/robots.txt` then confirms the transfer carried the value instead of being the thing that sets it. The FAIL direction holds over HTTP unconditionally — WordPress emits a virtual `Disallow: /` at `blog_public` = 0 — while the PASS direction needs the two-line signature, and the virtual file exists only when no PHYSICAL robots.txt sits in the webroot, which is exactly why `es_indexing_state()` reports that file's presence without parsing it. Half (a), the backup keys, is W1 or W3. **CORRECTION, measured on a live site and not reasoned: read the page's `<meta name="robots">`, NOT `/robots.txt`.** On a site with `blog_public` = 0 and Yoast active, `/robots.txt` served `User-agent: *` / `Disallow:` — allow everything — because Yoast filters `robots_txt` and replaces core's output entirely, while `es_indexing_state()` correctly reported `robots_file: null` (no PHYSICAL file). The same page emitted `<meta name='robots' content='noindex, nofollow' />`, which is the honest signal: Yoast respects the WordPress setting there. So the earlier instruction to fetch `/robots.txt` for this row gives a **FALSE PASS on any Yoast site**, and Yoast is on most of them. Fetch a page, read the meta, and treat a missing meta as UNVERIFIED rather than as permission. Keep reading `/robots.txt` for row 26's own question — whether a physical one blocks the site — but never infer `blog_public` from it | **auto** |
| 24 | The manifest still describes this site | What the project manifest recorded last session is still true of the site now | Call `es_manifest_verify()` and require an EMPTY drift list. It re-reads the recorded `pages` map (`slug => post_id`) and the recorded front page against the live site, and reports four things a second session would otherwise walk straight into: a page that no longer exists, a slug the manifest records that the live page no longer answers to — renamed by hand, or recorded under a key that was never a slug, and the row gives both facts rather than picking one outside this framework, a slug now answered by a DIFFERENT post id (deleted and recreated — the worst one, because everything looks fine), and a repointed front page. It reports drift and deliberately does NOT repair it: repairing means guessing which of the two truths was intended, and only a human knows. Any drift is a FAIL that blocks reuse of the recorded ids — writing into an id the manifest is wrong about is how a second session overwrites what a first one agreed to leave alone. An absent manifest is N/A on a first run, and a FAIL on any run that claims to continue a previous one. Bridge-free arm. A copy preserves post ids, so after a migration this row is answered by the plugin having imported the database at all — verify what is visible from outside instead: each slug in the recorded `pages` map resolves, and the served HTML carries the `page-id-<id>` body class matching the RECORDED id. That is rendered equivalence, never bit equivalence, and the difference is worth naming: a page that vanished or answers under a different id is caught, a `_elementor_data` that lost one invisible setting renders identically and is invisible to every HTTP request there is. Worlds: the call is W1 or W3; the body-class check is its W2 shadow | **auto** |
| 25 | A 404 that belongs to this site | Requesting a URL that does not exist returns status 404 AND a styled page with the site's header, footer and a way out | Request a definitely-missing path. Two independent failures live here and one hides the other: a **soft 404** returns status 200 with "not found" text, which tells a crawler the page is real and keeps it indexed forever; and a **naked 404** returns the right status over the theme's bare default, no header, no menu, dead end. Require BOTH — status exactly 404, and the header/footer fragments present with at least one navigation link out. Check the status before the markup: a 200 makes the markup irrelevant. After a transfer this row is where `.htaccess` surfaces. The rewrite rules live in a file, not the database, so they did not travel — and on nginx they never existed. Until Settings → Permalinks is saved once on production, every URL except the front page returns 404. It is the single most likely production-only failure of a migration, and it presents here first | **auto** |
| 26 | robots.txt, sitemap and favicon exist and agree | The three files every crawler and browser asks for on the first visit are present and do not contradict the site's intent | Request `/robots.txt`, the sitemap it points at, and the favicon. (a) robots.txt: 2xx, and it must NOT `Disallow: /` on a site meant to be indexed — cross-check against `blog_public` from row 23, because a site can be blocked by one, the other, or both, and fixing one while the other still blocks reads as "we fixed it". (b) The sitemap: 2xx, valid XML, and the URLs in it resolve — a sitemap listing dead or staging URLs is worse than none. Confirm robots.txt actually names it. (c) The favicon: a real request, not a `<link>` tag — a tag pointing at a missing file is the normal broken case, and browsers show the default anyway. All three are N/A only on a site explicitly not meant to be public, which must be stated | **auto** |
| 27 | The built palette is the approved palette | The accent and brand colours on the live site are the ones signed off in the mockup, not near-misses | Take the approved token values from the mockup/manifest and grep the compiled CSS and the Elementor kit for each one. Report exact matches, and report every near-miss — a hex one or two digits away is the signature of a colour re-typed by hand instead of taken from the token, and it is invisible to the eye and obvious to a diff. FAIL on an approved accent that appears NOWHERE: it means the build used something else. This is the only row that can catch a build that looks right and is not what the client approved. No recorded approved palette = UNVERIFIED, never PASS — comparing against nothing always passes | **auto** |
| 28 | No setting sits on the wrong element type | No widget carries container-only keys, and no container carries the underscore forms | `require_once` the sandbox's `es-builder.php` and call `es_container_audit()` on each page's saved `_elementor_data` — the same call as row 11, which already reports these as offenders. Elementor names the same control differently by location: a container takes `padding`, a widget takes `_padding`. **Measured on Elementor 4.2.2**: the wrong form never reaches the generated stylesheet at all — a widget with `_padding:33px` emitted a padding rule, one with `padding:44px` emitted nothing and the value appeared nowhere in `post-<id>.css`; a container with `_padding` likewise emitted nothing, as did `flex_direction` on a widget. It saves, opens and renders, and simply does not apply — visible in the source, absent on screen, so it gets re-added and re-wondered-at rather than found. The key list is deliberately short and derived from this library's own usage: `width` is excluded because it is both a container layout key and a real widget control, and an invented offender costs more than a missed one. W1 or W3 only, same as row 11, because it is the same call. A partial HTTP shadow exists — count `.e-con`/`.e-con-inner` nesting depth in the served HTML — but that reads RENDERED output where this row reads STORED data, so it corroborates and is not the row | **auto** |
| 29 | A button's label is centred inside the button, on both axes | Every button in scope renders its label centred horizontally AND vertically inside its own box | GEOMETRY on the built page — do NOT grep the CSS for this one. `text-align:center` centres only horizontally and is present in every failing case, so a text check passes while the page is visibly wrong; that is exactly how this shipped in all four mockup assets undetected. With the in-app browser, for every `.btn`, `<button>` and `[role=button]` on each page in scope: take the element's border box, then the bounding box of its own label ink (a `Range` over the element's contents — NOT the element rect, which is the thing being tested), and compare the two centres on each axis. FAIL any button whose label centre is more than 2px off the box centre on either axis; report the worst offender's `dy`/`dx` with the button's height, because the number grows with the stretch and a 2px miss and a 65px miss look identical in prose. Buttons stretch when they sit in a flex row (`align-items:stretch` is the default), in an equal-height card grid, or in a grid row taller than the button — so measure at every breakpoint in scope, not just desktop: the same button measured 0px off at 430 and 65.2px off at 1920 on the same page. Not reachable server-side and not provable from CSS, so a build where the browser cannot reach the host is UNVERIFIED, never PASS | **measured (in-app browser)** |
| 30 | A button's BOX is the size of its content, not of its container | Every button in scope renders at its own content height: `offsetHeight` is at most 1.5x the greater of (`lineHeight` + `paddingTop` + `paddingBottom` + `borderTop` + `borderBottom`) and its declared `min-height` | GEOMETRY on the built page, and it is the OTHER HALF of row 29 — run both, because each is blind exactly where the other looks. Row 29 measures the label inside the box. `.btn` here sets `align-items:center`, so a button stretched to 147px centres its label perfectly inside 147px: row 29 measures ~0px of offset and PASSES while the box is three times the height of the thing in it. Measured in `proof-direct-mockup.html`, where `.ctas` is a direct grid item of a row as tall as the hero image beside it — 91px at 1440x900 and 147px at 1920x1200 against a 49.2px content height, and row 29 green at both. **The `min-height` term is not padding for the threshold, it is intent:** a 44px WCAG 2.5.5 touch target on a 33.2px button is a declared floor, and measuring against the intrinsic height instead scores it 0.325 and turns an accessibility fix into a defect. **Threshold, measured not chosen:** across all five assets the largest LEGITIMATE stretch is 0.295 (a newsletter button matching the height of the input beside it, the same deliberate shape as `.searchbar` at 0.11), and the smallest DEFECT is 0.85. 0.5 is their geometric mean — equidistant in ratio terms from both, 1.7x clear either way. Report the worst ratio with its box and content heights, because the stretch grows with viewport height and one screenshot proves nothing. Measure at every breakpoint in scope. **Only trust a settled layout:** an unlaid-out page reports a 0 viewport, and a page whose images have not loaded reports boxes that are pure reflow noise — this rule was first measured mid-hydration and reported 293px buttons that were 38px a moment later. Refuse to score until `innerWidth > 0` and every `img` is `complete`. Not provable from CSS, so a build where the browser cannot reach the host is UNVERIFIED, never PASS | **measured (in-app browser)** |
| 31 | The build stands at the axis positions the mockup was approved at | The scale, density, elevation and ground the client signed off in the mockup are the ones the build actually resolved — and the ones that reached the stylesheet | Three layers, and the third one is what most of this row exists to keep honest. **(a) Declared — all four token axes.** Read the approved mockup's `:root`: `--type-ratio`, `--display-lh`, `--fs-h1-max` (scale), `--sp-scale` (density), `--elev-rest` (elevation), `--c-bg` / `--c-bg-alt` / `--c-text` (ground). Then `require_once` the sandbox's `es-builder.php` and call `es_tokens()` — the same route rows 11, 16, 22, 23, 24 and 28 take, and it must run BEFORE row 22 empties the sandbox — and compare each against `type_ratio`, `display_lh`, `fs_h1_max`, `sp_scale`, `elev_rest`, `bg`, `bg_alt`, `text`. Normalise whitespace and case first, and resolve any `var(--…)` inside `--elev-rest` against the mockup's OWN `:root` before comparing, because `hairline` is tabled as `0 0 0 1px var(--c-border)` where the builder holds a resolved value; a `var()` that will not resolve is UNVERIFIED for elevation, not a mismatch. FAIL on any difference, naming BOTH values — a framework default that survived the dialogue (`1.333` / `1.0` / `none` / `#FFFFFF` on a client who chose editorial, generous, soft-shadow and ink) is precisely what this row is for, and it is invisible to every other row here. **(b) Rendered — scale and elevation.** A value in `es_tokens()` is a value in a file, not on a page: a site built before the override was written passes (a) and is still wrong. Recompute with the build's OWN functions — never re-derive the formula in prose, for row 11's reason — and count the result in the compiled `post-<id>.css`: the h1's `font-size` must be `es_fs_at( 3, 1280 )` px and its `line-height` `es_t('display_lh')` em, and the card recipes' rest `box-shadow` must be `es_t('elev_rest')` verbatim. A zero count means the tokens never reached this page — cache, or a build that predates the override. **(c) Not covered, and say so per page rather than implying it.** *Density has no rendered layer*: `es_sp()` multiplies and rounds, so a padding number cannot be inverted back to its multiplier, and at `standard` (1.0) the scaled and unscaled values are identical — any count-based probe would pass on a build where density never moved at all. Report density as declared-only, UNVERIFIED for the rendered half, never PASS. *Composition is not machine-checkable here at all*: it has no token, because its position is which helper is called in what arrangement, so neither `es_tokens()` nor the stylesheet reveals it. Read the composition marker out of the mockup's `:root` — `/* composition: LP-ASYMMETRIC */` and friends — state that blueprint by name, and ask the user to confirm the built page follows it — it is the axis most able to drift in silence. Accent and brand colours stay row 27's; this row owns the ground trio, which row 27 never reads. No approved mockup in reach = UNVERIFIED, never PASS — comparing against nothing always passes. Worlds: (a) is W1 or W3, because `es_tokens()` is a library call; (b) is W2 or W1, because the compiled CSS is fetchable over HTTP. On a production with no ephemeral-PHP arm, (a) is UNVERIFIED there and has to be carried by the local pass alone | **auto (declared) + auto (rendered: scale, elevation) + eyes (composition)** |
| 32 | A page reads at the width the reader is actually on, not at 1280 | On a composition that bleeds to a viewport edge: the outer page margin holds ONE ratio across the whole range rather than growing with the screen, **only media reaches the glass**, the hero copy is vertically centred in a row a bleeding image sizes, no run of text comes within 16px of the viewport edge, and every `.canvas` on the strip spans the full viewport | GEOMETRY on the built page at **320 / 430 / 768 / 1024 / 1280 / 1440 / 1920 / 2560**, and the last two widths are the whole row. This shipped twice because every measurement stopped at 1920, where the defect is present but not yet ugly. **(a) Page margin — the RATIO, not a threshold.** Take the first content column line (`.services .items`, or the section head), divide its `left` by the viewport, and compare the answer ACROSS widths: a margin that grows monotonically is the defect even while every single reading is under whatever bar you set. That is how this shipped a second time — a cap took 2560 from 27.7% to 18.0%, cleared the 20% bar, and left 2000 reading 25.0% total with the direction unchanged; the reader said "de 2560px para abajo tiene que ser responsive, con márgenes reales" about the FIXED build. FAIL if the margin fraction at 2560 exceeds the fraction at **1920** by more than 1pp. **The lower sample used to be 1440 and that was wrong, in a way that only a legitimate value could expose.** The pathology is a margin that grows WITHOUT BOUND; the probe has to be two points that are both above whatever knee the band's floor produces, or it reads a transition that stops as growth that does not. A band with a `1140px` floor is PINNED at 1140 until `floor ÷ fraction` — 1341px at 85vw, but 1676px at 68vw — and inside that pinned segment the fraction necessarily climbs, because the band is fixed there by construction. Sampling 1440 puts one foot inside that segment. `clamp(1140px, 68vw, 100vw)` reads 10.4% at 1440 and 16.0% at 1920, 2000, 2560, 3440 and 5120 alike: it rises to a hard plateau at its knee and holds it forever, which is what this row asks for, and the old probe scored it +5.58pp and FAILED it. **The correction does not weaken the row — MUTATION, run on the gallery at 1920 and 2560:** fixed `1140px` **+7.42pp FAIL**, the rejected `clamp(1140px, calc(1140px + (100vw − 1280px)*0.5), 1600px)` **+6.77pp FAIL**, `clamp(1140px, 85vw, 100vw)` **0.00pp ok**, `clamp(1140px, 68vw, 100vw)` **0.00pp ok**. Every historical defect this row was built from still fails it. **Report the whole curve anyway** — 1280 / 1440 / 1920 / 2560 — because the plateau is the thing being asserted and a single delta cannot show it; a fraction still climbing at 2560 fails whatever the arithmetic says. A proportional band reads one constant above its knee — 7.5% per side at 85vw, 16.0% at 68vw; no capped band can. `full-end` IS the viewport edge, so the 14 named tracks must sum to the screen; cap the twelve columns with a fixed `--content-width` and the outer `1fr` gutter is the only track left to absorb a wider display. Measured on the gallery at a fixed `1140px`: 150.1 / 390.1 / **710.1px** at 1440 / 1920 / 2560 = 10.4% → **27.7%**, the composition drifting right by half of it. 20% is the line between a margin and a dead band, and it is what picks the `--content-width` cap. **(b) Hero void.** Compare the centre of a `Range` over the hero head's contents with the centre of its border box — the RANGE, not the element rect, for row 29's reason: the box is stretched to the row and reports 0 by construction. Measured `dy` −18.3px at 1920 and **−157.5px at 2560** on a row of 714.6px holding 399.5px of ink, because `height:100%` on the bleeding frame has no definite basis in an auto row and silently computes to `auto`, letting the image's own ratio size the hero. **(b2) ONLY MEDIA AT THE GLASS.** For every element whose border box reaches the layout viewport edge, ask what is inside it. A photograph is a bleed and is the blueprint working. A CARD is sliced — measured on the gallery at 2000 as a last card with its frame at right 2000.0 and its body ink at 1968.0, one object half bled and half not, which the reader called cut off twice. A FORM is a broken page — every field and the submit button of `.band .formwrap` ended at exactly x=2560.0 on a 2560 viewport with a 1453.3px name input, i.e. a control with no hit-slop on one side and a border coincident with the screen edge: *"esto no puede pasar bajo ningún concepto"*. Report the element, its composition, its text length and its control count; FAIL on any non-empty text or any `input`/`textarea`/`select`/`button` inside a box at the glass. `framework-audit.php`'s `RT_MOCKUP_BLEED_NOT_MEDIA` decides the same question statically from the selector's subject and runs offline; this arm is the one that sees what the CSS cannot say. **(c) Text at the edge.** Walk TEXT NODES, not elements, and take a `Range` per node: an element box can sit inside the viewport while its ink overflows it, which is exactly how `Restauración` sat 4.9px past a 1024 viewport. Skip anything inside an `overflow-x:auto` ancestor **including the element itself** — the strip's own `<pre>` is its own scroller and reads as 224px of clipped text otherwise. A photograph at the glass is a bleed; a paragraph at the glass is an amputation, and `documentElement.scrollWidth === clientWidth` throughout, so no overflow gate can see it. **(d) Canvas span.** `full-start`/`full-end` are only honest if the grid container is the viewport: a canvas carrying `margin-inline:auto` inside a grid parent shrink-wraps to max-content and every named-line bleed on that section becomes a silent no-op — measured 1244.6px wide at 1440, 1920 AND 2560 alike. **(e) NO WORD IS BROKEN MID-WORD.** Take a `Range` per WORD — not per text node, which is arm (c) — and count `getClientRects()`: more than one rectangle means that word was split across lines. **This is a SECOND GATE, not a variant of the overflow one, and the difference is the whole reason it exists: a mid-word chop produces exactly ZERO overflow, because the chop is what prevents it.** `documentElement.scrollWidth === clientWidth` reads perfectly clean on a page whose headline renders `PIEZA / S QUE / NO SE / REPIT / EN`. MEASURED on tuscapas at 320: the chop hit an `h1` on all SEVEN pages while the overflow arm reported 0 at every width, and a human reading the render found it before the sweep did. The cause is `overflow-wrap:anywhere`, which is REQUIRED under 1024 for 320px reflow and which display type cannot survive; the fix and its measurements are in `html-mockup/references/mockup-guide.md` § "An overflow check cannot see a chopped word". **Separate a hyphenated break from a raw one before reporting it**, or a correctly hyphenated word is filed as a defect: measure the first line’s rendered width against the natural width of the substring it holds AND against that substring plus a hyphen — on tuscapas that read 92.9px against 92.9 / 99.8, so the break was RAW and `hyphens:auto` was doing nothing at all, matching what `_build-gallery.php:7692` already recorded about the headless renderer’s missing Spanish dictionary. Report the element and the word. **Only trust a settled layout**, per row 30: refuse to score until `innerWidth > 0` and every `img` is `complete`. Not provable from CSS and not reachable server-side — `framework-audit.php`'s `RT_MOCKUP_BLEED_FIXED_BAND` decides only whether the token is a bare literal — so a build where the browser cannot reach the host is UNVERIFIED, never PASS | **measured (in-app browser)** |
| 33 | The sandbox is gone BEFORE the export runs | `wp-content/novamira-sandbox/` is empty at the moment the migration plugin packages the site | The migration tool exports `wp-content` wholesale and has no idea this directory means anything — `es_sandbox_dir()` is `WP_CONTENT_DIR . '/novamira-sandbox'`, so it travels with everything else and `es-builder.php` lands on the client's server reachable by URL. No off-the-shelf plugin knows to leave it out, which is exactly why this is a row here and not a setting there. The proof is row 22 run at the RIGHT MOMENT: `es_sandbox_purge()` then `es_sandbox_report()` returning empty, immediately before the export, not merely at some point during the build. Ordering is the whole rule — a sandbox emptied and then used again for one more fix is a sandbox that ships. Corroborate afterwards over HTTP: request `/wp-content/novamira-sandbox/es-builder.php` on production and read the STATUS CODE only. **404 is the only PASS**; **403 is not** — it proves the server declines to serve, not that the file is absent; **200 is a FAIL, empty body included**, which is the trap, because `es-builder.php` exits without `ABSPATH` and a naive probe reads that empty 200 as "nothing there". Calibrate first with a random 32-hex filename: if THAT returns 200 the host soft-404s and this row is UNVERIFIED. Worlds: the purge is W1, the probe is W2. **Built directly on the client's WordPress through the connector = N/A**, and say N/A rather than leaving it blank: nothing is exported on that route, so there is no packaging step to get wrong, and row 22 already requires the sandbox empty at hand-off. This row exists for the copy, where the ordering constraint is new and no plugin enforces it. **Check the exclusion filter too, not only the purge.** All-in-One exposes `ai1wm_exclude_content_from_export`, and a mu-plugin adding `novamira-sandbox` to it makes the exclusion mechanical instead of remembered. Verify THREE things about that snippet, because two of them fail silently: the path is relative to wp-content (`novamira-sandbox`, not a full path), it carries NO trailing slash (`novamira-sandbox/` is a different entry and never matches), and it lives in `wp-content/mu-plugins/` where it cannot be deactivated by accident. A filter present but misspelled reads exactly like a filter that works. MEASURED against All-in-One 7.x free by running its own export enumerator: with the sandbox holding two files, `novamira-sandbox` excludes both, while `novamira-sandbox/`, `/novamira-sandbox` and `wp-content/novamira-sandbox` each exclude NOTHING and say nothing — three of the four natural spellings are silent no-ops. What that measurement does not cover is the finished archive: the free plugin refuses `wp ai1wm backup` as an Unlimited-only feature, so no `.wpress` was produced. The purge therefore stays the arm that blocks and the filter stays corroboration | **auto** |
| 34 | Production's runtime is not older than the one QA ran on | The PHP, WordPress, Elementor and Elementor Pro production runs are at or above what the build was verified against | Read the `build` section of the manifest — `es_build_fingerprint()` recorded it at T0. An older Elementor on the destination refuses controls the build wrote, so the page renders wrong while every other row here stays green, which is exactly why this is a row and not a note. WordPress and Elementor are partly readable over HTTP (the generator meta, Elementor's own stamp in the generated CSS); **the PHP version is invisible over HTTP, full stop** — report it UNVERIFIED unless a W3 arm exists, and never infer it. A production BELOW local is a FAIL; above is a note worth stating, because Elementor moving under a site nobody rebuilt is its own hazard. **Built directly through the connector = N/A**: there is one site, so the runtime the build ran on IS the runtime it serves from, and comparing it against itself proves nothing. The row is about a destination that was never the machine QA ran on | **auto (wp, elementor) + unverifiable over HTTP (php)** |
| 35 | The store is open to a visitor, not behind a placeholder | No URL on the delivered site renders a "coming soon" screen instead of its own content | **HTTP, no login, no library — the cheapest row in this table and it catches a delivery-blocking defect.** WooCommerce has shipped Launch Your Store since 9.1: a fresh install starts at `woocommerce_coming_soon = yes` and only flips when a human completes the onboarding wizard, which a build script never does. The switch is an option, options are database rows, and a migration copies the database — so the store arrives at the destination hidden. Fetch every page in scope plus `/tienda/`, `/carrito/`, `/finalizar-compra/` and `/mi-cuenta/`, and FAIL any response carrying `woocommerce-coming-soon` on the `<body>` class. **Do not use the status code and do not use the page's own `<h1>`**: the placeholder answers a perfectly ordinary 200 and carries a plausible heading of its own, so a reachability probe and a heading probe both pass on a shop no customer can buy from. **`woocommerce_store_pages_only = yes` is why this needs its own row rather than a glance:** the placeholder then covers ONLY the store, so the home page, the about page and the contact form — every URL a person clicks first — render perfectly. MEASURED on a real migration (2026-09-11): of twelve URLs probed, ten rendered their own content — home 200 with 28 Elementor elements and its real `<h1>`, the custom 404 firing — while the other two, `/tienda/` and `/carrito/`, answered 200 with zero Elementor elements and the `<h1>` "Great things are on the horizon". Nothing else in this table looked at those two URLs. Check `woocommerce_private_link` in the same pass. **Built directly through the connector = still applicable**, not N/A: WooCommerce defaults to coming-soon on any fresh install, migration or not, so the switch is an install artefact rather than a transfer one. Worlds: W1 or W2, and it needs neither the library nor a credential in either | **auto** |

## The three worlds a row can be checked in

**Only one of the two build routes needs this section.** Building DIRECTLY on the client's
WordPress through the NovaMira connector — the default, and the only route for an existing site —
gives one site with full PHP execution on it, so every row below runs in one place and there is no
split to reason about. Read the rest of this section only when the site was built locally and
copied to a production that has no connector.

On that route the split is real, and naming the world stops being bookkeeping: a row that reads as
checkable everywhere and is checkable nowhere is exactly how UNVERIFIED quietly becomes PASS.

- **W1 — local, before the transfer.** Full PHP, full browser, no connector needed, because it is
  your own filesystem. **The most capable of the three**, and where the whole pass runs. For most
  rows this is easier than what they were written for: reaching a localhost URL is trivial where
  reaching a NovaMira sandbox host is documented below as unverified.
- **W2 — production over HTTP.** Roughly half these rows run unchanged against any reachable URL.
- **W3 — production by ephemeral PHP.** `wp eval-file - < es-builder.php` runs the library against
  production **writing nothing to disk**. The question is not "is there SSH?" but *can one PHP file
  be evaluated against production's WordPress leaving nothing behind* — SSH with wp-cli yes, a
  cPanel terminal yes, phpMyAdmin alone no.

**Rows compose across worlds, and the composition is the row.** Row 16 is the pattern: layer (a)
is options, proven in W1; layer (b) is the served HTML, proven in W2; neither alone is the row.
The delivery gate reads that composition and never one world by itself.

**Without a W3 arm, rows 11, 28 and 31(a) have no production arm at all.** They read stored data,
and no HTTP request can see it. The local pass is what carries them, and the migration plugin copying the site faithfully is the assumption underneath — an assumption, stated as one, not a check.
Report UNVERIFIED for production and say which rows, because **UNVERIFIED is still not a pass** and
inventing a fourth verdict to cover this would be the one thing this file exists to prevent.

## Honest limits — do not dress these up

- **Row 1 is a live read, never an assumption.** No skill in this repo sets `woocommerce_currency`
  — grep confirms zero hits across every skill. A build inherits whatever the store already had,
  which on a fresh WooCommerce install is USD. So read the live value and flag USD; never infer
  euros from the fact that the design used `€`.
- **Row 8 behaviour, and row 10 geometry, are not provable from HTML/CSS text.** The stored
  setting and the CSS rules only prove intent. Report them as such.
- **Row 16 needs the build to tell you WHICH page is the home.** The options say which page is the
  front page; they cannot say whether it is the right one. Without the id the build reported, this
  row degrades to "some page is the front page", which passes happily on a site whose front page
  still belongs to the previous agency. Ask for the id — never assume the slug is `inicio`, `home`
  or `portada`.
- **Rows that depend on WooCommerce** (1, 2) do not apply to a build without WooCommerce — mark
  them N/A, not PASS.
- **Rows 13–15 need headless Chrome to REACH the URL.** Whether it reaches a NovaMira sandbox host
  is UNVERIFIED — the in-app browser is usually policy-blocked there, and headless Chrome is a
  different path that has not been tried yet. If it cannot connect, the script prints
  `UNREACHABLE` and runs nothing: report that as a tooling gap, never as a failing audit or a
  score of zero. Record the answer here the first time you find out.
- **`UNREACHABLE` is flaky on Windows — re-run before believing it.** Confirmed on the dev machine:
  the same public URL alternates between a clean run and
  `EPERM, Permission denied: …\Temp\lighthouse.<n>` with nothing changed in between, and two
  consecutive runs failed while the next succeeded. The script now uses a unique Chrome profile per
  attempt and retries three times with a backoff, which reduces it but does not eliminate it
  (antivirus holding the temp profile is the likely cause). Never report a site as unreachable off
  a single failed run.
- **Row 30 covers four axes of five, and two of them only halfway.** Scale and elevation are
  proven twice — declared and rendered. Ground and density are proven once, at the declaration
  only. Composition is not proven at all: it is named to the user and left to their eyes. That is
  the honest extent of "the build matches the approved mockup", and it is written here because the
  alternative — a row implying five and testing two — is the failure this row was added to remove.
- **A Lighthouse score is not a visual sign-off.** 100/100 on a page that looks broken is still a
  broken page. These rows add a floor, they do not replace the user's eyes.
- **The performance number is NOISY — treat one run as an estimate.** Five runs against the SAME url on the same
  machine scored 95, 74, 71, 69 and 97 — a 28-point swing from lab variance alone (LCP 2.5-5.0 s). Never report a
  performance delta from single runs on different machines or networks; take the best of three on
  the same machine, and say so. Accessibility, best-practices and SEO are near-deterministic —
  they inspect the DOM instead of timing it, which is why they are the ones that block.
| 36 | Nothing asks Google for the typeface | The served HTML of every page in scope requests neither `fonts.googleapis.com` nor `fonts.gstatic.com` | Call `es_front_font_probe( '<url>' )` per page: it fetches that URL and reads the bytes the visitor gets, which is the only place this can be proven at all. The build's own `es_font_serving_check()` cannot — it reads the style registry, and a build runs in REST/CLI where the front end's enqueues never fire, so its honest answer there is `sin-confirmar` forever and THIS row is what clears it. **A non-200, or a body with no closed document, is UNVERIFIED and never PASS**: a 401, a 500, an empty body and a maintenance page all contain zero occurrences of `googleapis`, so "not found" is worth nothing until the bytes are known to be the page's — the probe refuses to answer `limpio` without both, and that refusal is the point of it. `limpio` is a statement about the URL FETCHED and not about the site, which is why this runs per page like every other per-page row. Two needles because they are two requests: the stylesheet, and the font file a bad self-hosting job or a stray `preconnect` still pulls out of the same third country. A `google` verdict is a FAIL and a legal one — every visitor's IP to a third country with no consent and no legal basis, with EU rulings against the site's owner and not Google's — and the remedy is `elementor-core/references/knowledge.md`, "Servir las familias tipograficas", never a consent banner. Distinct from row 20, whose part (c) asks whether the cookie policy NAMES the hosts the site loads; this one asks that the font host not be loaded in the first place. **What it cannot see:** a font pulled in by JavaScript at runtime leaves no Google URL in the served HTML, so a server-side fetch calls that page clean — row 21 is the one that catches it, with a real browser listing the outbound requests, and this row is the cheap pass that runs everywhere rather than the last word. Worlds: W1 and W3 make the library call; in W2 fetch the page and count the two strings by hand, which needs no library and is the same proof | **auto** |

## Divi

Rows 1, 3, 4, 6, 7, 9 read WordPress options or plain front HTML and are builder-agnostic —
they run unchanged on Divi. Row 16 is builder-agnostic in layer (a), which is pure WordPress
options; its layer (b) body-class check is Elementor-specific, so on Divi match the `page-id-<id>`
class alone and say that is what you matched.

Row 11 is builder-agnostic as a RULE but not as a METHOD: `es_container_audit()` reads
`_elementor_data`, which Divi does not have. On a Divi build, count nesting depth in the rendered
front HTML instead (`.et_pb_section` > `.et_pb_row` > `.et_pb_column` > module) and report it as
**UNVERIFIED** until the Divi storage format is confirmed and written into
`divi-core/references/gotchas.md`. Row 12 (one H1) reads plain front HTML — it runs unchanged.

Rows 2, 5, 8, 10 lean on Elementor artifacts (`elementor-menu-cart`, the nav-menu widget
attribute, the stored `sticky` control, `post-<id>.css`). The Divi equivalents are NOT confirmed
in this repo — `divi-core` is a scaffold and its own gotchas file lists its storage facts as
"verify before trusting". On a Divi build, report those rows as **UNVERIFIED**: not a PASS, not
a FAIL. Confirm the real Divi artifact names by introspection on the site, then append them to
`divi-core/references/gotchas.md` so the next run can actually check them.
